<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pim_RaizesUrbanas"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name="ui.ResumoPedidoActivity"
            android:exported="false" />
        <activity
            android:name="ui.TelaPedidoActivity"
            android:exported="false" />
        <activity
            android:name="ui.DetalheProdutoActivity"
            android:exported="false" />
        <activity
            android:name="ui.CarrinhoActivity"
            android:exported="false" />
        <activity
            android:name="ui.DetalheClienteActivity"
            android:exported="false" />
        <activity
            android:name="ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ui.CadastroClienteActivity" />
        <activity android:name="ui.ListaProdutosActivity" />
        <activity android:name="ui.EditarPerfilActivity" />

    </application>

</manifest>